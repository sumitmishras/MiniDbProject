<UserControl x:Class="MiniDbWpf.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             >
    <ScrollViewer>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,60,0,0">
            <Border Width="72" Height="72" CornerRadius="18" Background="#7C3AED"
                    HorizontalAlignment="Center" Margin="0,0,0,16">
                <TextBlock Text="🗄️" FontSize="36" HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>

            <TextBlock Text="{Binding AppName}" FontSize="24" FontWeight="Bold"
                       Foreground="White" TextAlignment="Center"/>
            <TextBlock Text="{Binding Version}" FontSize="16" Foreground="#888"
                       TextAlignment="Center" Margin="0,4,0,16"/>

            <Border Style="{StaticResource CardBorder}" Width="480">
                <StackPanel>
                    <TextBlock Text="{Binding Description}" Foreground="#CCC" FontSize="14"
                               TextWrapping="Wrap" Margin="0,0,0,16"/>
                    <TextBlock Text="{Binding TechStack}" Foreground="#888" FontSize="12"
                               TextAlignment="Center"/>
                </StackPanel>
            </Border>

            <TextBlock Text="© 2024 Mini Database Project" Foreground="#555" FontSize="11"
                       TextAlignment="Center" Margin="0,20,0,0"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
