<UserControl x:Class="MiniDbWpf.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:material="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:conv="clr-namespace:MiniDbWpf.Converters">
    <UserControl.Resources>
        <conv:LogLevelToColorConverter x:Key="LogLevelColor"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="System Logs" Style="{StaticResource PageTitleStyle}"/>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox ItemsSource="{Binding Levels}" SelectedItem="{Binding SelectedLevel}"
                          Background="#1E1E32" Foreground="White" BorderBrush="#3A3A5C"
                          Width="120" Height="36" FontSize="13" Padding="8,4"/>

                <TextBox Grid.Column="2" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Background="#1E1E32" Foreground="White" BorderBrush="#3A3A5C"
                         BorderThickness="1" Padding="10,8" FontSize="13"
                         Height="36"/>

                <StackPanel Grid.Column="4" Orientation="Horizontal">
                    <Button Content="Export" Background="#3A3A5C" Foreground="White"
                            BorderThickness="0" Height="36" Cursor="Hand" Padding="16,0"
                            material:ButtonAssist.CornerRadius="8" Margin="0,0,8,0"
                            Command="{Binding ExportCommand}"/>
                    <Button Content="Refresh" Background="#7C3AED" Foreground="White"
                            BorderThickness="0" Height="36" Cursor="Hand" Padding="16,0"
                            material:ButtonAssist.CornerRadius="8"
                            Command="{Binding RefreshCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0">
            <DataGrid ItemsSource="{Binding Logs}" AutoGenerateColumns="False"
                      Background="Transparent" BorderThickness="0"
                      HeadersVisibility="Column" RowHeaderWidth="0"
                      Foreground="{DynamicResource MaterialDesignBody}"
                      GridLinesVisibility="None" FontSize="13"
                      SelectionMode="Single" CanUserSortColumns="True">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#1E1E32"/>
                        <Setter Property="Foreground" Value="#AAA"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#3A3A5C"/>
                        <Setter Property="Padding" Value="8,10"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                    </Style>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="8,6"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="TIMESTAMP"
                                        Binding="{Binding Timestamp, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}"
                                        Width="160"/>
                    <DataGridTextColumn Header="LEVEL" Width="100"
                                        Binding="{Binding Level}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Foreground"
                                        Value="{Binding Level, Converter={StaticResource LogLevelColor}}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="CATEGORY" Binding="{Binding Category}" Width="140"/>
                    <DataGridTextColumn Header="MESSAGE" Binding="{Binding Message}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
